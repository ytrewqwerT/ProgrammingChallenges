
#include <stdio.h>

int main(void) {
    int T; scanf("%d", &T);
    for (int t = 1; t <= T; t++) {
        int N, M; scanf("%d %d", &N, &M);

        printf("Case #%d: %d\n", 1, 2);
    }

    return 0;
}
